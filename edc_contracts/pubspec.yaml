name: edc_contracts
description: Shared wire DTOs and risk normalization for the Safe Basket backend and EDC client.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0

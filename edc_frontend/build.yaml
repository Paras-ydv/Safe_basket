# Emit/parse JSON in snake_case to match the Safe_basket backend contract
# (edc_contracts wire format). Applies to all json_serializable models.
targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake

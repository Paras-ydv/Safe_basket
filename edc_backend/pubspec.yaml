name: edc_backend
description: Dart Frog backend for Safe Basket EDC analysis.
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.2.6
  dotenv: ^4.2.0
  http: ^1.2.0
  mime: ^1.0.4
  postgres: ^3.3.0
  edc_matcher:
    path: ../edc_matcher

dev_dependencies:
  dart_frog_gen: ^2.1.0
  dart_frog_test: ^0.1.1
  test: ^1.24.0
